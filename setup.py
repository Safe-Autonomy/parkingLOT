#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='parkinglot',
    version='0.0.1',
    description='parkinglot',
    packages=['parkinglot'],
    install_requires=[]
)